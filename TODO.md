# Task: Fix server running on port 3002 instead of 3000 and "compiled with problems"

## Steps:
- [x] 1. Create TODO.md with plan breakdown (done)
- [x] 2. Edit frontend-web/package.json: Change start script to use default port 3000 (verified with create_file)
- [x] 3. Test: cd frontend-web && npm install && npm start (verify port 3000, check compilation warnings)
- [x] 4. If TS warnings persist: Analyze specific errors and fix TS files (index.tsx fixed, deps installed, Login.tsx typed, a11y improved)
- [x] 5. Mark complete and attempt_completion

